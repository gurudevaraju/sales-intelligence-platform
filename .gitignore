# OS
.DS_Store

# Python
__pycache__/
*.pyc
.ipynb_checkpoints/

# Data: only the raw uncompressed JSONL export is excluded (~69 GB,
# regenerate/re-download separately). Its compressed .zst copy (~9 GB) and
# the smaller generated outputs (filtered CSVs, schema cache) are tracked.
data_prep/data/raw_data/test_scans.json

# Large generated outputs (exceed GitHub's 100 MB per-file limit).
# Regenerate locally via the pyspark pipeline notebooks instead of tracking.
data_prep/data/filtered_data/vulnerable_cves.csv
data_prep/data/filtered_data/hosts_scored.csv
data_prep/data/filtered_data/vulnerable_hosts.csv

# Secrets / local env config
.env
.env.*
!.env.example

# Virtualenvs
.venv/
venv/
